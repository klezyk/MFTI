# Рецензия на работу студента 1709041996

## Задание 11.1 — Свёрточные и полносвязные нейросети

---

### Критерий 1: Вопрос 1. Сколько обучаемых параметров у получившейся модели?
**Оценка: 2/2**

Студент дал полный ответ с развёрнутым объяснением: «Веса имеют размерность 2 на 1 — это 2 параметра. Смещение имеет размерность 1 на 1 — это 1 параметр. Итого: 2 + 1 = 3 обучаемых параметра.» Разбиение на weights и bias показывает понимание устройства линейного слоя. Числовое значение 3 верное, обоснование исчерпывающее.

---

### Критерий 2: Вопрос 2. Сколько итераций потребовалось, чтобы алгоритм сошелся?
**Оценка: 2/2**

Ответ студента: 1593 итерации, что подтверждается выводом ячейки: `Итерация: 1593. Сходимость. Остановка итераций после 27 эпох.` Ранняя остановка реализована корректно через критерий сравнения разности средних потерь с tolerance. Число итераций получено из реального прогона.

---

### Критерий 3: Вопрос 3. Какое accuracy получается после обучения?
**Оценка: 3/3**

Полученная точность 0.8852 (88.52%) полностью совпадает с эталонной метрикой LogReg moons ~0.8852. Использованы `nn.BCEWithLogitsLoss()` и `torch.optim.SGD(lr=0.05)`, что является корректной связкой для бинарной классификации с логитами. Функция `predict()` применяет sigmoid и порог 0.5 внутри `@torch.no_grad()`, что методически правильно.

---

### Критерий 4: Вопрос 4. Какая из активаций показала наивысший accuracy?
**Оценка: 3/3**

Код обучения реализован качественно: функции `fit_one_epoch()`, `evaluate_one_epoch()` и `training_function()` с переключением `model.train()`/`model.eval()` и `torch.no_grad()` на валидации. Протестированы все 5 активаций (Identity 0.9168, ReLU 0.9765, LeakyReLU 0.9751, Tanh 0.9695, ELU 0.9792), построен сравнительный график. Вывод «Наивысшую точность показала модель ELU: Accuracy = 97.92%» корректен и соответствует численным метрикам.

---

### Критерий 5: Вопрос 5. Как описать действия ядер свёрток?
**Оценка: 2/2**

Все 6 пар сопоставлены правильно: Размытие—Е, Резкость—Д, Тождественное—А, Вертикальные—В, Горизонтальные—Б, Все границы—Г, что полностью соответствует эталонному ключу A3, Б5, В4, Г6, Д2, Е1. Каждое ядро применено через `show_convolution_transformation()` и результаты визуализированы, что подтверждает обоснованность ответа.

---

### Критерий 6: Вопрос 6. Какое accuracy LeNet после обучения (2 знака)?
**Оценка: 3/3**

Архитектура LeNet реализована модульно (без Sequential) с правильной последовательностью слоёв: Conv2d(1→6,k=3) → MaxPool → Conv2d(6→16,k=3,padding=1) → MaxPool → Flatten → Linear(576→120) → Linear(120→84) → Linear(84→10). Обучение проведено на 10 эпохах с `Adam(lr=0.001)` и `CrossEntropyLoss`, итоговая точность 0.9891 (98.91%) превосходит эталон 0.9884. Хотя padding=1 во втором Conv отличается от классической LeNet, архитектура согласована по размерностям и даёт отличный результат.

---

### Критерий 7: Дополнительные баллы (бонус)
**Оценка: 3/3**

LeakyReLU реализована корректно: forward через `torch.where(data > 0, data, self.alpha * data)`, backward возвращает 1 при x>0 и α при x≤0. Tanh: forward `torch.tanh(data)`, backward `1 - torch.tanh(data)**2` — правильная производная. Оба класса прошли `torch.allclose` тесты как для forward, так и для backward pass против эталонных реализаций PyTorch. Визуализация графиков forward и gradient построена для обеих функций.

---

## Общие комментарии

**Общее впечатление:** Отличная работа, выполненная на максимальный балл. Все три части задания (LogReg на moons, FC MNIST, LeNet CNN) проработаны полностью, все 6 вопросов получили верные ответы, бонусная часть реализована качественно с проверкой через `torch.allclose`.

**Сильные стороны:**
- Архитектуры моделей полностью соответствуют требованиям ТЗ: ручная `LinearRegression` через `nn.Parameter`, FC с двумя скрытыми слоями по 128 нейронов, модульная LeNet.
- Метрики превосходят эталонные: FC-ELU 0.9792 против эталона 0.9768, LeNet 0.9891 против эталона 0.9884, LogReg 0.8852 точно совпадает.
- Бонус выполнен качественно: LeakyReLU и Tanh с корректными forward/backward, оба прошли allclose-тесты против PyTorch.
- Код хорошо структурирован, подробно прокомментирован на русском языке, использованы правильные практики PyTorch (`model.train()`/`model.eval()`, `torch.no_grad()`, `copy.deepcopy()` для сохранения параметров).
- Все графики построены: сравнение 5 активаций, визуализация 6 ядер свёрток, итоговое сравнение всех моделей включая LeNet.

**Недостатки:** Существенных недостатков не обнаружено. Можно отметить только минорный косметический момент: в заголовке одного из графиков Tanh указано «Custom Leaky ReLU» (опечатка от копипасты), но на оценку это не влияет.

**Рекомендации:** Работа выполнена на высоком уровне — рекомендаций по улучшению нет. Студент продемонстрировал уверенное владение PyTorch, понимание устройства линейных и свёрточных слоёв, корректную реализацию backward pass для кастомных активаций.

---

**Итого: 18/18 баллов**
