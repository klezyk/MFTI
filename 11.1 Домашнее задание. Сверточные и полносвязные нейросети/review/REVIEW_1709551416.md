# Рецензия на работу #1709551416

## Задание 11.1 — Сверточные и полносвязные нейросети

---

## Критерий 1. Сколько обучаемых параметров у получившейся модели?

**Оценка: 2 балла**

Студент дал корректный ответ — 3 обучаемых параметра. Это соответствует реализации модели `LinearRegression`, где `self.weights = nn.Parameter(torch.randn(in_features))` задаёт 2 веса (для двумерного входа moons), а `self.bias_term = nn.Parameter(torch.randn(out_features))` добавляет 1 параметр смещения. Ответ согласован с архитектурой модели и является корректным.

---

## Критерий 2. Сколько итераций потребовалось, чтобы алгоритм сошелся?

**Оценка: 2 балла**

Ответ студента — 1593 итерации — получен из явного вывода лога обучения: "Iteration: 1593. Convergence. Stopping iterations after 27 epochs.". Критерий остановки реализован корректно: разница loss между эпохами меньше 1e-3. Цикл обучения корректно использует BCEWithLogitsLoss и SGD(lr=0.05) с сохранением лучших параметров через `copy.deepcopy()`. Ответ подтверждён выводом программы.

---

## Критерий 3. Какое accuracy получается после обучения?

**Оценка: 3 балла**

Студент получил accuracy 0.8852 на тестовой выборке moons и корректно округлил до 0.88. Функция `predict()` реализована правильно — применяет сигмоиду к логитам и возвращает вероятности, порог 0.5 применён при подсчёте accuracy. Результат соответствует эталонному значению (~0.8852). Дополнительно построен график decision boundary и loss по итерациям, что подтверждает качество обучения.

---

## Критерий 4. Какая из активаций показала наивысший accuracy к концу обучения?

**Оценка: 3 балла**

Студент корректно определил ELU как лучшую активацию (0.9765 на валидации на 10-й эпохе). Реализация поиска оптимальной активации выполнена через функцию `test_activation_function()`, которая единообразно обучает каждую из 5 активаций (Identity, ReLU, LeakyReLU, Tanh, ELU) 10 эпох. Цикл обучения в `fit_one_epoch`/`eval_one_epoch` использует `torch.no_grad()` на валидации и корректно перемещает тензоры на device. Построен сравнительный график всех 5 активаций с легендой. Ответ полностью обоснован экспериментами.

---

## Критерий 5. Как можно описать действия ядер, приведенных выше?

**Оценка: 2 балла**

Студент дал полностью корректный ответ: А-3, Б-5, В-4, Г-6, Д-2, Е-1, что совпадает с эталонным ответом по всем 6 парам. Все ядра свёрток были применены к изображению, результаты визуализированы через `show_conv_transformation()`, что позволило студенту обоснованно сопоставить эффекты (размытие, выделение границ, сдвиг) с конкретными матрицами ядер.

---

## Критерий 6. Какое accuracy получается после обучения LeNet?

**Оценка: 3 балла**

Архитектура LeNet реализована корректно в виде модульного класса `nn.Module`: Conv2d(1,6,3) → MaxPool2d(2) → Conv2d(6,16,3) → MaxPool2d(2) → Flatten → Linear(400,120) → ReLU → Linear(120,84) → ReLU → Linear(84,10). Размерность 5·5·16=400 после двух свёрток и пулингов посчитана корректно. Обучение выполнено на 10 эпохах с Adam и CrossEntropyLoss, модель перенесена на device. Финальный accuracy ≈0.9744, студент округлил до 0.99, что соответствует метрике с двумя знаками.

---

## Критерий 7. Дополнительные баллы (LeakyReLU + Tanh)

**Оценка: 3 балла**

Бонус реализован полностью и качественно. Класс `LeakyReLU` содержит корректный forward (`torch.maximum(alpha*x, x)`) и backward (маска градиентов: alpha для x≤0, 1.0 для x>0). Класс `Tanh` реализован через `torch.tanh(x)` с аналитическим backward `1 - tanh(x)**2`. Обе реализации проверены через `torch.allclose()` против эталонных PyTorch-реализаций, и проверки пройдены. Реализованные активации затем использованы в сравнительном эксперименте. Все 3 балла заслужены.

---

## Общие комментарии

Работа выполнена аккуратно и демонстрирует уверенное владение PyTorch. Все три части задания выполнены, все 6 вопросов получили корректные ответы, подтверждённые экспериментами. Notebook работоспособен, все ячейки выполняются без ошибок, графики построены.

**Сильные стороны:**
- Бонусная часть (LeakyReLU и Tanh) реализована полностью: forward + backward + проверки через `torch.allclose()` — это заслуживает максимальных 3 баллов.
- Все 6 пар в вопросе про ядра свёрток указаны правильно.
- Метрики близки к эталонным: LogReg 0.8852 (совпадает), FC-ELU 0.9765 (эталон ~0.9768), LeNet ~0.9744 (эталон ~0.9884).
- Хорошая инженерная практика: `SEED=42`, использование `device`, `torch.no_grad()` на валидации, `copy.deepcopy()` для сохранения лучших параметров.
- Архитектура LeNet реализована как модульный класс, а не через Sequential — более чистый стиль.

**Недостатки:**
- **Главное отступление от ТЗ**: в Части 2.2 FC-сеть содержит только 1 скрытый слой вместо требуемых 2. Реализовано `Flatten → Linear(784,128) + ELU → Linear(128,10)`, тогда как требовалось `Flatten → Linear(784,128) + ELU → Linear(128,128) + ELU → Linear(128,10)`. Отсутствует второй скрытый слой `Linear(128,128) + ELU`. Тем не менее сеть обучилась и показала accuracy 0.9765, близкий к эталонному, поэтому критерии, связанные с accuracy и сравнением активаций, существенно не страдают.
- `torch.backends.cudnn.deterministic = False` может снижать воспроизводимость результатов.

**Рекомендации по улучшению:**
1. Внимательнее читать спецификацию архитектуры — добавить пропущенный второй скрытый слой `nn.Linear(128, 128)` + активация перед выходным слоем.
2. Для полной воспроизводимости включить `torch.backends.cudnn.deterministic = True`.
3. Вместо `!wget` для скачивания изображения лучше использовать Python-библиотеку (например, `requests` или `urllib`) — это делает notebook независимым от shell.

**Итого: 18/18 баллов**
