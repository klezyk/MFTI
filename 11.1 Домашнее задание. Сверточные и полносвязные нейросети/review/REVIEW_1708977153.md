# Рецензия на работу студента 1708977153

## Задание 11.1 — Свёрточные и полносвязные нейросети

---

### Критерий 1: Вопрос 1. Сколько обучаемых параметров у получившейся модели?
**Оценка: 2/2**

Ответ студента: 3 параметра. Класс `LinearRegression` реализован через `nn.Parameter(torch.randn(in_features, out_features))` для весов и `nn.Parameter(torch.zeros(out_features))` для смещения. При in_features=2, out_features=1 получается 2 веса + 1 bias = 3 обучаемых параметра, что соответствует эталонному ответу.

---

### Критерий 2: Вопрос 2. Сколько итераций потребовалось, чтобы алгоритм сошелся?
**Оценка: 2/2**

Ответ студента: 1652 итерации, подтверждается выводом: «Iteration: 1652. Convergence. Stopping iterations after 28 epochs.» Ранняя остановка реализована корректно: сравнение разницы средних лоссов между эпохами с `tol=1e-3`, используется `copy.deepcopy(model.state_dict())` для сохранения параметров модели при сходимости.

---

### Критерий 3: Вопрос 3. Какое accuracy получается после обучения?
**Оценка: 3/3**

Полученное accuracy на тестовом наборе moons составило 0.8852, что точно соответствует эталонному результату. Использованы `nn.BCEWithLogitsLoss()` и `torch.optim.SGD(lr=0.05)` — методически правильная связка. Функция `predict()` корректно применяет `torch.sigmoid` с порогом 0.5 внутри декоратора `@torch.no_grad()`. Построены график loss и визуализация decision boundary.

---

### Критерий 4: Вопрос 4. Какая из активаций показала наивысший accuracy?
**Оценка: 3/3**

Ответ: ELU (accuracy=0.9772). Архитектура FC-сети полностью соответствует требованиям: `Flatten → Linear(784,128) → activation → Linear(128,128) → activation → Linear(128,10)` — два скрытых слоя по 128 нейронов. Функции `fit_one_epoch()`, `eval_one_epoch()` и `train_func()` реализованы корректно с правильным переключением режимов обучения и валидации (`model.train()` / режим валидации) и использованием `torch.no_grad()`. Протестированы все 5 активаций (Identity 0.9169, ReLU 0.9738, LeakyReLU 0.975, Tanh 0.9596, ELU 0.9772), построен сравнительный график.

---

### Критерий 5: Вопрос 5. Как описать действия ядер свёрток?
**Оценка: 2/2**

Ответ студента: А:3, Б:5, В:4, Г:6, Д:2, Е:1 — все 6 пар совпадают с эталонным ключом A3, Б5, В4, Г6, Д2, Е1. Каждое из 6 ядер применено через `show_conv_transformation()` к тестовому изображению, результаты визуализированы, что обосновывает сделанный выбор.

---

### Критерий 6: Вопрос 6. Какое accuracy LeNet после обучения (2 знака)?
**Оценка: 3/3**

Ответ: 0.99 (фактическое значение 0.9884 на 10 эпохе). Архитектура LeNet реализована модульно как класс (не Sequential) с корректной последовательностью слоёв: Conv2d(1→6, k=3) → ReLU → MaxPool(2,2) → Conv2d(6→16, k=3) → ReLU → MaxPool(2,2) → Flatten → Linear(400→120) → ReLU → Linear(120→84) → ReLU → Linear(84→10). Обучение на 10 эпохах с `Adam` и `CrossEntropyLoss`, итоговая метрика 0.9884 превосходит эталон.

---

### Критерий 7: Дополнительные баллы (бонус)
**Оценка: 3/3**

LeakyReLU реализована корректно: forward через `torch.maximum(self.alpha * x, x)`, backward возвращает тензор единиц с заменой на alpha в точках x≤0. Tanh: forward `torch.tanh(x)`, backward `1 - torch.tanh(x)**2` — правильная производная. Обе реализации прошли проверку `torch.allclose` против эталонных `F.leaky_relu` / `torch.tanh` как для forward, так и для backward pass. Обе кастомные активации затем использованы в общем сравнении 5 активаций, что подтверждает работоспособность.

---

## Общие комментарии

**Общее впечатление:** Работа выполнена на максимальный балл. Все три части задания (LogReg на moons, FC MNIST с анализом активаций, LeNet CNN) выполнены полностью, все 6 вопросов получили верные ответы с конкретными численными значениями, бонусная часть реализована качественно и верифицирована через `torch.allclose`.

**Сильные стороны:**
- Архитектуры моделей полностью соответствуют требованиям: ручная `LinearRegression` через `nn.Parameter`, FC с двумя скрытыми слоями по 128 нейронов, модульная LeNet как класс.
- Метрики на уровне или выше эталона: LogReg 0.8852 (точно), FC-ELU 0.9772, LeNet 0.9884.
- Бонус полностью выполнен: LeakyReLU и Tanh с корректными forward/backward, оба прошли allclose-тесты.
- Установлен `SEED=42`, включены `torch.backends.cudnn.deterministic` и `torch.use_deterministic_algorithms(True)` для воспроизводимости.
- Правильные практики PyTorch: переключение режимов обучения/валидации, `torch.no_grad()` на валидации, `copy.deepcopy()` для сохранения лучших весов, использование device (CUDA/CPU).
- Все графики построены: loss на moons, decision boundary, сравнение 5 активаций, визуализация 6 ядер свёрток, итоговое сравнение всех моделей с LeNet.

**Недостатки:** Существенных недостатков не обнаружено.

**Рекомендации:** Работа выполнена на высоком уровне, рекомендаций по улучшению нет. Студент уверенно владеет PyTorch, понимает устройство линейных и свёрточных слоёв, корректно реализует backward pass для кастомных активаций.

---

**Итого: 18/18 баллов**
