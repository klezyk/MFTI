# Рецензия на работу 1687971717

## Критерий 1: Часть 1. Анализ и предобработка данных

**Оценка: 1/2**

Основные этапы предобработки данных выполнены: загрузка train/test, анализ пропущенных значений через `isna()`, логарифмическое преобразование popularity, создание признака boolDeadRelations, разделение age на age_value и age_no_data, группировка culture с one-hot кодированием, удаление столбцов с высокой долей пропусков. Также проведено one-hot кодирование house по топ-12 категориям и построена матрица корреляций. Однако в заданиях, требующих анализа и интерпретации ("проанализировать..."), отсутствуют развёрнутые текстовые выводы — например, нет интерпретации корреляционной матрицы, нет объяснения выбора порога для удаления столбцов, нет анализа распределений признаков. Формально предобработка проведена достаточно полно, но отсутствие выводов не позволяет поставить максимальный балл.

## Критерий 2: Часть 2. Обучение моделей

**Оценка: 0/2**

Обучена только одна модель — LogisticRegression с параметрами по умолчанию, показавшая Accuracy = 0.7917. При этом задание предполагает обучение нескольких моделей (RandomForest, KNN, DecisionTree и др.), подбор гиперпараметров через GridSearchCV, использование Pipeline и StandardScaler, а также кросс-валидацию. Ничего из этого не выполнено. Кроме того, ConvergenceWarning (lbfgs не сошёлся за 100 итераций) не был исправлен — можно было увеличить max_iter или использовать другой solver. Фактически выполнена лишь минимальная часть задания по обучению, что недостаточно даже для 1 балла, поскольку отсутствует сравнение моделей как таковое.

## Критерий 3: Часть 3. Оцените качество моделей

**Оценка: 1/1**

Оценка качества модели выполнена: рассчитан accuracy_score на валидационной выборке (0.7917), выбрана лучшая модель (единственная — LogisticRegression), корректно применена предобработка к тестовой выборке, сгенерирован файл submission (new_submission_1.csv). Несмотря на то что сравнения нескольких моделей нет (так как обучена только одна), формальные требования по оценке качества и генерации submission выполнены. Задание по этому критерию можно считать закрытым.

## Общие комментарии

**Общее впечатление**: работа выполнена частично. Первая часть (предобработка) сделана на хорошем уровне — видно, что студент разобрался с feature engineering и корректно обработал большинство признаков. Однако вторая часть (обучение моделей) практически не выполнена.

**Сильные стороны**:
- Грамотная предобработка: группировка culture, log-transform popularity, разделение age, one-hot кодирование
- Корректное разделение данных с стратификацией (stratify=y)
- Консистентная обработка train и test
- Создан файл submission

**Главные недостатки**:
- Обучена только одна модель вместо нескольких — это главный пробел работы
- Отсутствует подбор гиперпараметров (GridSearchCV)
- Нет кросс-валидации и Pipeline
- Нет текстовых выводов и интерпретаций в аналитических заданиях
- Не исправлен ConvergenceWarning

**Рекомендации по улучшению**:
1. Обучить минимум 3-4 модели (RandomForest, KNN, DecisionTree, LogisticRegression) и сравнить их по accuracy
2. Использовать Pipeline(StandardScaler(), model) для корректного масштабирования
3. Применить GridSearchCV для подбора гиперпараметров лучшей модели
4. Добавить кросс-валидацию (cross_val_score) для более надёжной оценки
5. Писать текстовые выводы после каждого аналитического блока — это важная часть работы data scientist'а

**Итого: 2/5 баллов**
